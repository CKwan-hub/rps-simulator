// REMOVE
// import React from 'react';

// const Icon = ({ type, id, position, direction }) => {
//   const { x, y } = position;

//   return (
//     <div
//       className={`icon ${type}`}
//       style={{
//         left: `${x}px`,
//         top: `${y}px`,
//         transform: `translate(-50%, -50%)`,
//       }}
//     >
//       {/* You can customize the icon's appearance based on type */}
//       {type === 'rock' && <div className="rock-icon">🪨</div>}
//       {type === 'paper' && <div className="paper-icon">📄</div>}
//       {type === 'scissors' && <div className="scissors-icon">✂️</div>}
//     </div>
//   );
// };

// export default Icon;
